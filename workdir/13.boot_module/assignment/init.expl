int main()
{
decl
    int temp,num;
enddecl
begin
	temp = exposcall ( "Write" , -2, "INIT" );
	while(1==1) do
		temp = 1;
		// not doing anything. But terminating will stop the RR in timer interrupt as init will call RET and stop the process
	endwhile;
    return 0;
end
}